const add = (first, second) =>{
   return first + second;
}
// export for one things
// export default add;

const multiply = (first, second) =>{
return first*second;
}

// to export one more things
export {add, multiply}